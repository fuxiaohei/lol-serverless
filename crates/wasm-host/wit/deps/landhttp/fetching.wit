package land:http@0.2.0;

interface fetching {
    use types.{request,response,request-error,request-options};
    // send request function
    send-request: func(req: request, options: request-options) -> result<response,request-error>;
}